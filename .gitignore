# PhpStorm / JetBrains IDE 相关
.idea/
*.iml
*.ipr
*.iws

# PHP 项目通用依赖与缓存
# Composer 依赖包（不提交，通过 composer install 重建）
/vendor/
# 本地 Composer 可执行文件（不提交）
composer.phar
# 应用类项目(type=project)建议提交 composer.lock 以锁定依赖版本，故不忽略

# 环境配置与敏感信息
.env
.env.*.local
*.local

*.log
*.cache
*.tmp
